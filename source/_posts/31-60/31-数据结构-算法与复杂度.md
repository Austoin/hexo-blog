---
title: 考研《数据结构》第一章：绪论（二）算法与算法评价
date: 2026-03-14 21:30:00
updated: 2026-03-14 22:00:00
tags: [考研, 数据结构, 算法]
categories: 数据结构
description: 数据结构考研复习笔记第一章第二节,涵盖算法五大特性、时间复杂度与空间复杂度分析、大O记法、递归空间开销,含18道历年真题详解
cover: /img/cover/cover31.webp
top_img: /img/cover/cover31.webp
---

## 算法基本概念

**程序 = 数据结构 + 算法**
- 数据结构:程序要处理的信息
- 算法:处理信息的步骤

### 算法五大特性(必须全部具备)

1. **有穷性**:必须在有穷步后结束,每步在有穷时间内完成
   - 算法必须有穷,程序可以无穷(如操作系统)
2. **确定性**:相同输入产生相同输出,无二义性
3. **可行性**:操作可通过基本运算有限次实现
4. **输入**:0个或多个输入
5. **输出**:1个或多个输出

### 好算法的标准

1. 正确性:正确解决问题
2. 可读性:逻辑清晰易懂
3. 健壮性:能处理非法输入
4. 高效率:时间复杂度低
5. 低存储:空间复杂度低

## 时间复杂度

### 计算规则(大O记法)

**加法规则**(并列循环):$O(f(n)) + O(g(n)) = O(\max(f(n), g(n)))$
- 例:先后执行一重循环和二重循环,总复杂度为$O(n^2)$

**乘法规则**(嵌套循环):$O(f(n)) \times O(g(n)) = O(f(n) \times g(n))$
- 例:外层循环n次,内层循环$\log_2 n$次,总复杂度$O(n\log_2 n)$

### 常见复杂度排序(必背)

口诀:**"常对幂指阶"**

$$O(1) < O(\log_2 n) < O(n) < O(n \log_2 n) < O(n^2) < O(n^3) < O(2^n) < O(n!) < O(n^n)$$

### 三种情况

- **最坏时间复杂度**:考研通常只关注最坏情况,代表算法底线
- **平均时间复杂度**:所有输入等概率出现的期望运行时间

### 对数阶推导

```c
int i = 1;
while (i <= n) {
    i = i * 2;
}
```

设执行x次,则$2^x > n$,解得$x = \log_2 n$,复杂度为$O(\log_2 n)$

## 空间复杂度

### 原地工作

只声明有限个局部变量,与问题规模n无关,空间复杂度为$O(1)$

### 递归空间开销

递归调用时系统开辟函数调用栈,每次递归压入新栈帧

**结论**:递归算法空间复杂度 = 递归深度

例:求n阶乘递归深度为n,空间复杂度$O(n)$

## 课后真题

### 单项选择题

**01.** 算法五大特性包括( )
- A. 可维护性、可读性、可行性
- B. 可行性、确定性、有穷性
- C. 确定性、有穷性、可靠性
- D. 可读性、正确性、可行性

**02.** 下列关于算法的说法正确的是( )
- A. 算法时间效率取决于执行所花时间
- B. 不允许用空间换时间
- C. 算法必须具备有穷性、确定性等五个特性
- D. 通常用时间效率和空间效率衡量算法优劣

**03.** 某算法时间复杂度为$O(n^2)$,则表明该算法( )
- A. 问题规模是$n^2$
- B. 执行时间等于$n^2$
- C. 执行时间与$n^2$成正比
- D. 问题规模与$n^2$成正比

**04.** 若算法空间复杂度为$O(1)$,则表示该算法( )
- A. 不需要任何辅助空间
- B. 所需辅助空间大小与问题规模n无关
- C. 不需要任何空间
- D. 所需空间大小与问题规模n无关

**05.** 下列时间复杂度最小的是( )
- A. $T_1(n) = n\log_2 n + 5000n$
- B. $T_2(n) = n^2 - 8000n$
- C. $T_3(n) = n\log_2 n - 6000n$
- D. $T_4(n) = 20000\log_2 n$

**06.** 时间复杂度( )
```c
void fun(int n) {
    int i=1; 
    while(i<=n) 
        i=i*2; 
}
```
A. $O(n)$  B. $O(n^2)$  C. $O(n\log_2n)$  D. $O(\log_2n)$

**07.** 时间复杂度( )
```c
void fun(int n) { 
    int i=0;
    while(i*i*i<=n) 
        i++;
}
```
A. $O(n)$  B. $O(n\log_2n)$  C. $O(\sqrt{n})$  D. $O(n^{1/3})$

**08.** 最后一行语句频度在最坏情况下是( )
```c
for(i=n-1; i>1; i--)
    for(j=1; j<i; j++)
        if(A[j]>A[j+1])
            A[j]与A[j+1]对换;
```
A. $O(n)$  B. $O(n\log_2n)$  C. $O(n^3)$  D. $O(n^2)$

**09.** 时间复杂度( )
```c
if (n>=0) {
    for (int i=0; i<n; i++)
        for (int j=0; j<n; j++)
            printf("输入数据大于或等于零\n");
} else {
    for (int j=0; j<n; j++)
        printf("输入数据小于零\n");
}
```
A. $O(n^2)$  B. $O(n)$  C. $O(1)$  D. $O(n\log_2n)$

**10.** 下划线语句执行次数( )
```c
int m=0, i, j;
for(i=1; i<=n; i++)
    for(j=1; j<=2*i; j++)
        m++;
```
A. $n(n+1)$  B. $n$  C. $n+1$  D. $n^2$

**11.** 时间复杂度( )
```c
int Func(int n) {
    if(n==1) return 1;
    else return 2*Func(n/2)+n;
}
```
A. $O(n)$  B. $O(n\log_2n)$  C. $O(\log_2n)$  D. $O(n^2)$

**12.【2011真题】** 时间复杂度( )
```c
x=2;
while(x<n/2)
    x=2*x;
```
A. $O(\log_2n)$  B. $O(n)$  C. $O(n\log_2n)$  D. $O(n^2)$

**13.【2012真题】** 时间复杂度( )
```c
int fact(int n) {
    if(n<=1) return 1;
    return n*fact(n-1);
}
```
A. $O(\log_2n)$  B. $O(n)$  C. $O(n\log_2n)$  D. $O(n^2)$

**14.【2014真题】** 时间复杂度( )
```c
count=0;
for(k=1; k<=n; k*=2)
    for(j=1; j<=n; j++)
        count++;
```
A. $O(\log_2n)$  B. $O(n)$  C. $O(n\log_2n)$  D. $O(n^2)$

**15.【2017真题】** 时间复杂度( )
```c
int func(int n) {
    int i=0, sum=0;
    while(sum<n) sum += ++i;
    return i;
}
```
A. $O(\log_2n)$  B. $O(n^{1/2})$  C. $O(n)$  D. $O(n\log_2n)$

**16.【2019真题】** 时间复杂度( )
```c
x=0;
while (n>=(x+1)*(x+1))
    x=x+1;
```
A. $O(\log_2n)$  B. $O(n^{1/2})$  C. $O(n)$  D. $O(n^2)$

**17.【2022真题】** 时间复杂度( )
```c
int sum=0;
for(int i=1; i<n; i*=2)
    for(int j=0; j<i; j++)
        sum++;
```
A. $O(\log_2n)$  B. $O(n)$  C. $O(n\log_2n)$  D. $O(n^2)$

**18.【2025真题】** 时间复杂度( )
```c
int count=0, i, j;
for(i=1; i*i<=n; i++)
    for(j=1; j<=i; j++)
        count++;
```
A. $O(\log_2n)$  B. $O(n)$  C. $O(n\log_2n)$  D. $O(n^2)$

### 综合应用题

**01.** 求时间复杂度

① 
```c
i=1; k=0;
while(i<n-1) {
    k=k+10*i;
    i++;
}
```

②
```c
y=0;
while((y+1)*(y+1)<=n)
    y=y+1;
```

③
```c
for(i=0; i<n; i++)
    for(j=0; j<m; j++)
        a[i][j]=0;
```

## 答案与解析

### 选择题

**01. B** 算法五大特性:有穷性、确定性、可行性、输入、输出

**02. C** 算法必须具备五个特性

**03. C** 大O符号表示执行时间与$n^2$成正比

**04. B** 辅助空间大小与问题规模n无关

**05. D** 对数阶最小

**06. D** i每次乘2,执行$\log_2 n$次

**07. D** $i^3 \le n$,即$i \le n^{1/3}$

**08. D** 等差数列求和$\sum_{i=2}^{n-1} (i-1) = \frac{(n-2)(n-1)}{2}$,最高阶$n^2$

**09. A** 最坏情况执行嵌套循环$n^2$

**10. A** $\sum_{i=1}^{n} 2i = n(n+1)$

**11. B** 主定理:$T(n) = 2T(n/2) + n$,共$\log_2 n$层,每层$O(n)$

**12. A** x每次乘2,执行$\log_2 n$次

**13. B** 递归深度n,每次常数操作

**14. C** 外层$\log_2 n$次,内层n次

**15. B** 循环k次后$sum = \frac{k(k+1)}{2} \ge n$,解得$k \approx \sqrt{n}$

**16. B** $(x+1)^2 > n$,即$x \approx \sqrt{n}$

**17. B** 总次数$1 + 2 + 4 + \dots + 2^k = 2^{k+1} - 1 < 2n$,为$O(n)$

**18. B** $\sum_{i=1}^{\sqrt{n}} i = \frac{\sqrt{n}(\sqrt{n}+1)}{2} = \frac{n}{2} + \frac{\sqrt{n}}{2}$,为$O(n)$

### 综合题

① $O(n)$ - 循环n次,每次常数操作

② $O(\sqrt{n})$ - $(y+1)^2 > n$时结束

③ $O(m \times n)$ - 双层独立嵌套循环

## 进阶:复杂度变形

若将第18题内层改为`j<=i*i`:

```c
for(i=1; i*i<=n; i++)
    for(j=1; j<=i*i; j++)
        count++;
```

**推导过程**

外层循环:i从1到$\sqrt{n}$

内层循环:对每个i执行$i^2$次

总次数:$T(n) = 1^2 + 2^2 + 3^2 + \dots + (\sqrt{n})^2$

使用平方和公式:$\sum_{k=1}^{m} k^2 = \frac{m(m+1)(2m+1)}{6}$

代入$m = \sqrt{n}$:

$$T(n) \approx \frac{\sqrt{n}(\sqrt{n}+1)(2\sqrt{n}+1)}{6}$$

最高阶项:$\sqrt{n} \times \sqrt{n} \times 2\sqrt{n} = 2n^{3/2}$

**答案**:$O(n\sqrt{n})$ 或 $O(n^{1.5})$
